[project]
name = "laia"
version = "0.1.0"
description = "Local Agent Isolation Architecture"
readme = "README.md"
requires-python = ">=3.15"
dependencies = []

[dependency-groups]
dev = [
    "markdown-it-py>=4.2.0",
    "ruff>=0.15.15",
]
